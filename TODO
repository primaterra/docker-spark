- create and test CI docker stage
- create and test CI/CD pipeline
- define src/ code structure for scalability, include databricks pipelines
- create sphinx config sh file
- create script to identify package updates in repo



# Test sphinx build:
cd Projects/docker-spark/

docker run --rm \
  -v $(pwd):/app \
  -w /app \
  --user root \
  my-ci-image sh -c '
    rm -f docs/source/modules.rst docs/source/*_*.rst &&
    sphinx-apidoc -o docs/source src --force --separate &&
    sphinx-build -b html docs/source docs/_build/html -v
  '
